///@description Initializes a new 2D array to given lengths with given default

/// @param row
/// @param col
/// @param default

var arr;
for (var i = argument0 - 1; i >= 0; i--)
	for (var j = argument1 - 1; j >= 0; j--)
		arr[i, j] = argument2;
return arr;