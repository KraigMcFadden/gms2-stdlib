/// Does the same thing as move, but takes in a point array with deltaX, deltaY

/// @param obj
/// @param deltaArray
/// @param collisionObj

var deltaX = argument1[point.X];
var deltaY = argument2[point.Y];

move(argument0, deltaX, deltaY, argument2);